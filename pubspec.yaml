name: education_tile_career_saathi
description: Education Tile Career Saathi App

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^latest
  firebase_auth: ^latest
  cloud_firestore: ^latest

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
